# CSII · UI Lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 從零建立個人 UI 實驗場 scaffold，含 Registry 自動索引機制，並完成第一個實驗（border-image 靜態三變體）。

**Architecture:** Shell / Registry / Experiments 三層，Registry 透過 `import.meta.glob` 在 build time 掃描實驗資料夾自動組出路由與索引。Shell 與 Experiments 僅透過 Registry 耦合。

**Tech Stack:** Vite 5 + React 18 + TypeScript (strict) + Tailwind v3 + CSS Modules + React Router v6 + Vitest + ESLint + Prettier + Husky

**Spec reference:** [docs/superpowers/specs/2026-04-15-ui-lab-design.md](../specs/2026-04-15-ui-lab-design.md)

**Working directory:** `d:/git/CSII/` (Windows, git-bash shell, not yet a git repo)

---

## Task 0: Initialize git repo

**Files:**
- Create: `d:/git/CSII/.gitignore`

- [ ] **Step 1: Initialize git repo**

Run: `cd /d/git/CSII && git init -b main`
Expected: `Initialized empty Git repository`

- [ ] **Step 2: Create .gitignore**

Create `d:/git/CSII/.gitignore`:

```gitignore
# Dependencies
node_modules/
.pnp.*

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*.local

# Test
coverage/
```

- [ ] **Step 3: Commit**

```bash
git add .gitignore
git commit -m "chore: init repo"
```

---

## Task 1: Vite + React + TypeScript scaffold

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `vite.config.ts`
- Create: `index.html`
- Create: `src/main.tsx`
- Create: `src/vite-env.d.ts`

- [ ] **Step 1: Create package.json**

```json
{
  "name": "csii-ui-lab",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx",
    "format": "prettier --write .",
    "test": "vitest",
    "prepare": "husky"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@typescript-eslint/eslint-plugin": "^8.8.1",
    "@typescript-eslint/parser": "^8.8.1",
    "@vitejs/plugin-react": "^4.3.2",
    "autoprefixer": "^10.4.20",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "eslint-plugin-react-refresh": "^0.4.12",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "postcss": "^8.4.47",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
```

- [ ] **Step 2: Create tsconfig.json (strict)**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    "baseUrl": ".",
    "types": ["vite/client", "vitest/globals"]
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 3: Create tsconfig.node.json**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 4: Create vite.config.ts**

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
  },
});
```

- [ ] **Step 5: Create index.html**

```html
<!doctype html>
<html lang="zh-TW">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CSII · UI Lab</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 6: Create src/main.tsx (temporary stub)**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './styles/globals.css';

const root = document.getElementById('root');
if (!root) throw new Error('Root element not found');

createRoot(root).render(
  <StrictMode>
    <div>Bootstrapping...</div>
  </StrictMode>,
);
```

- [ ] **Step 7: Create src/vite-env.d.ts**

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 8: Create placeholder globals.css**

Create `src/styles/globals.css`:

```css
/* Tailwind directives added in Task 2 */
html, body, #root {
  height: 100%;
  margin: 0;
}
```

- [ ] **Step 9: Install dependencies**

Run: `cd /d/git/CSII && npm install`
Expected: `added ... packages` and `node_modules/` exists.

- [ ] **Step 10: Verify dev server starts**

Run: `npm run dev`
Expected: Vite prints `Local: http://localhost:5173/`.
Action: Open URL, confirm "Bootstrapping..." renders. Kill with Ctrl+C.

- [ ] **Step 11: Commit**

```bash
git add package.json package-lock.json tsconfig.json tsconfig.node.json vite.config.ts index.html src/main.tsx src/vite-env.d.ts src/styles/globals.css
git commit -m "chore: scaffold vite + react + typescript strict"
```

---

## Task 2: Tailwind CSS setup

**Files:**
- Create: `tailwind.config.ts`
- Create: `postcss.config.js`
- Modify: `src/styles/globals.css`
- Create: `src/styles/theme.css`

- [ ] **Step 1: Create tailwind.config.ts**

```ts
import type { Config } from 'tailwindcss';

export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        accent: 'var(--color-accent)',
        surface: 'var(--color-surface)',
        'surface-2': 'var(--color-surface-2)',
        ink: 'var(--color-ink)',
        'ink-muted': 'var(--color-ink-muted)',
        border: 'var(--color-border)',
      },
      fontFamily: {
        sans: ['Inter', 'ui-sans-serif', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'ui-monospace', 'monospace'],
      },
    },
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 2: Create postcss.config.js**

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 3: Create src/styles/theme.css**

```css
:root {
  --color-accent: #6366f1;
  --color-surface: #0b0b0f;
  --color-surface-2: #15151c;
  --color-ink: #e8e8ef;
  --color-ink-muted: #9999a8;
  --color-border: #2a2a36;
}
```

- [ ] **Step 4: Replace src/styles/globals.css**

```css
@import './theme.css';
@tailwind base;
@tailwind components;
@tailwind utilities;

html,
body,
#root {
  height: 100%;
  margin: 0;
}

body {
  background-color: var(--color-surface);
  color: var(--color-ink);
  font-family: theme('fontFamily.sans');
}
```

- [ ] **Step 5: Verify Tailwind works**

Edit `src/main.tsx` temporarily, replace the `<div>Bootstrapping...</div>` with:

```tsx
<div className="p-8 text-accent">Tailwind works</div>
```

Run: `npm run dev`
Expected: Text appears indigo (`#6366f1`) with padding. Kill server.
Revert the `<div>` to `Bootstrapping...` before committing.

- [ ] **Step 6: Commit**

```bash
git add tailwind.config.ts postcss.config.js src/styles/theme.css src/styles/globals.css
git commit -m "chore: configure tailwind + theme variables"
```

---

## Task 3: ESLint + Prettier

**Files:**
- Create: `.eslintrc.cjs`
- Create: `.eslintignore`
- Create: `.prettierrc`
- Create: `.prettierignore`
- Create: `.vscode/settings.json`
- Create: `.vscode/extensions.json`

- [ ] **Step 1: Create .eslintrc.cjs**

```js
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
    'prettier',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    '@typescript-eslint/no-unused-vars': [
      'error',
      { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
    ],
  },
};
```

- [ ] **Step 2: Create .eslintignore**

```
dist/
node_modules/
coverage/
```

- [ ] **Step 3: Create .prettierrc**

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

- [ ] **Step 4: Create .prettierignore**

```
dist/
node_modules/
coverage/
package-lock.json
```

- [ ] **Step 5: Create .vscode/settings.json**

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "tailwindCSS.experimental.classRegex": [
    ["cva\\(([^)]*)\\)", "[\"'`]([^\"'`]*).*?[\"'`]"]
  ]
}
```

- [ ] **Step 6: Create .vscode/extensions.json**

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "bradlc.vscode-tailwindcss"
  ]
}
```

- [ ] **Step 7: Run lint + format**

Run: `npm run lint`
Expected: No errors (may have 0 warnings).

Run: `npm run format`
Expected: `.` files formatted, no errors.

- [ ] **Step 8: Commit**

```bash
git add .eslintrc.cjs .eslintignore .prettierrc .prettierignore .vscode/settings.json .vscode/extensions.json
git commit -m "chore: configure eslint + prettier + vscode"
```

---

## Task 4: Husky + lint-staged

**Files:**
- Modify: `package.json` (add lint-staged config)
- Create: `.husky/pre-commit`

- [ ] **Step 1: Initialize Husky**

Run: `cd /d/git/CSII && npx husky init`
Expected: `.husky/pre-commit` created with content `npm test`.

- [ ] **Step 2: Replace .husky/pre-commit**

Overwrite `.husky/pre-commit`:

```sh
npx lint-staged
```

- [ ] **Step 3: Add lint-staged config to package.json**

Add this top-level key to `package.json` (after `scripts`):

```json
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,css,md}": ["prettier --write"]
  },
```

- [ ] **Step 4: Verify pre-commit hook fires**

Create a throwaway file with bad formatting but valid code (no unused vars):

Run: `echo "export const    hookProbe  =   1" > src/_throwaway.ts`
Run: `git add src/_throwaway.ts && git commit -m "test hook"`
Expected: lint-staged runs, prettier reformats the file to `export const hookProbe = 1;\n`, commit succeeds. Inspect `src/_throwaway.ts` — it should be cleanly formatted.

- [ ] **Step 5: Clean up throwaway**

Run: `git rm src/_throwaway.ts && git commit -m "chore: remove hook test file"`
Expected: file removed and commit made. (Two commits in history: "test hook" + "chore: remove hook test file" — that's fine, proves the hook round-trip works.)

- [ ] **Step 6: Commit Husky config**

```bash
git add .husky/pre-commit package.json
git commit -m "chore: add husky pre-commit with lint-staged"
```

---

## Task 5: Registry types

**Files:**
- Create: `src/registry/types.ts`

- [ ] **Step 1: Create src/registry/types.ts**

```ts
import type { LazyExoticComponent, ComponentType } from 'react';

export type Category = 'css-techniques' | 'layouts' | 'components';

export type Status = 'wip' | 'done' | 'archived';

export interface ExperimentMeta {
  slug: string;
  category: Category;
  title: string;
  description: string;
  status: Status;
  tags: string[];
  createdAt: string;
  Component: LazyExoticComponent<ComponentType>;
}

export type ExperimentModule = {
  default: Omit<ExperimentMeta, 'slug' | 'category'>;
};

export const CATEGORIES: readonly Category[] = [
  'css-techniques',
  'layouts',
  'components',
] as const;

export const CATEGORY_LABELS: Record<Category, string> = {
  'css-techniques': 'CSS Techniques',
  layouts: 'Layouts',
  components: 'Components',
};
```

- [ ] **Step 2: Verify type-checks**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/registry/types.ts
git commit -m "feat(registry): add experiment meta types"
```

---

## Task 6: Registry loader — failing test

**Files:**
- Create: `src/registry/parseExperimentPath.ts`
- Create: `src/registry/parseExperimentPath.test.ts`

The glob-scanning side is hard to unit-test cleanly. We extract the **pure logic** (parsing a path into `{slug, category}`) and test that.

- [ ] **Step 1: Create src/registry/parseExperimentPath.test.ts (failing)**

```ts
import { describe, it, expect } from 'vitest';
import { parseExperimentPath } from './parseExperimentPath';

describe('parseExperimentPath', () => {
  it('extracts category and slug from a valid path', () => {
    const result = parseExperimentPath(
      '/src/experiments/css-techniques/border-image/meta.ts',
    );
    expect(result).toEqual({
      category: 'css-techniques',
      slug: 'border-image',
    });
  });

  it('accepts layouts category', () => {
    const result = parseExperimentPath(
      '/src/experiments/layouts/landing-page/meta.ts',
    );
    expect(result).toEqual({ category: 'layouts', slug: 'landing-page' });
  });

  it('accepts components category', () => {
    const result = parseExperimentPath(
      '/src/experiments/components/fancy-button/meta.ts',
    );
    expect(result).toEqual({
      category: 'components',
      slug: 'fancy-button',
    });
  });

  it('throws on unknown category', () => {
    expect(() =>
      parseExperimentPath('/src/experiments/random-thing/foo/meta.ts'),
    ).toThrow(/unknown category/i);
  });

  it('throws on malformed path', () => {
    expect(() => parseExperimentPath('/totally/wrong.ts')).toThrow();
  });
});
```

- [ ] **Step 2: Create src/registry/parseExperimentPath.ts (stub)**

```ts
export function parseExperimentPath(_path: string): never {
  throw new Error('not implemented');
}
```

- [ ] **Step 3: Run test — verify failure**

Run: `npm test -- --run`
Expected: 5 tests fail with "not implemented".

- [ ] **Step 4: Commit**

```bash
git add src/registry/parseExperimentPath.ts src/registry/parseExperimentPath.test.ts
git commit -m "test(registry): add failing tests for path parser"
```

---

## Task 7: Registry loader — implementation

**Files:**
- Modify: `src/registry/parseExperimentPath.ts`

- [ ] **Step 1: Implement parseExperimentPath**

Replace `src/registry/parseExperimentPath.ts`:

```ts
import { CATEGORIES, type Category } from './types';

export interface ParsedPath {
  category: Category;
  slug: string;
}

export function parseExperimentPath(path: string): ParsedPath {
  const parts = path.split('/').filter(Boolean);
  const expIdx = parts.indexOf('experiments');
  if (expIdx === -1 || parts.length < expIdx + 3) {
    throw new Error(`malformed experiment path: ${path}`);
  }
  const category = parts[expIdx + 1] as string;
  const slug = parts[expIdx + 2] as string;
  if (!CATEGORIES.includes(category as Category)) {
    throw new Error(`unknown category "${category}" in path: ${path}`);
  }
  return { category: category as Category, slug };
}
```

- [ ] **Step 2: Run test — verify passes**

Run: `npm test -- --run`
Expected: 5 tests pass.

- [ ] **Step 3: Commit**

```bash
git add src/registry/parseExperimentPath.ts
git commit -m "feat(registry): implement experiment path parser"
```

---

## Task 8: Registry glob loader

**Files:**
- Create: `src/registry/loadExperiments.ts`
- Create: `src/registry/index.ts`

The glob side is implemented but not unit-tested (Vite-specific, validated via integration).

- [ ] **Step 1: Create src/registry/loadExperiments.ts**

```ts
import type { ExperimentMeta, ExperimentModule, Category } from './types';
import { parseExperimentPath } from './parseExperimentPath';

const modules = import.meta.glob<ExperimentModule>(
  '/src/experiments/*/*/meta.ts',
  { eager: true },
);

export const experiments: ExperimentMeta[] = Object.entries(modules)
  .map(([path, mod]) => {
    const { category, slug } = parseExperimentPath(path);
    return { ...mod.default, slug, category };
  })
  .sort((a, b) => b.createdAt.localeCompare(a.createdAt));

export function byCategory(cat: Category): ExperimentMeta[] {
  return experiments.filter((e) => e.category === cat);
}

export function findExperiment(
  category: string,
  slug: string,
): ExperimentMeta | undefined {
  return experiments.find((e) => e.category === category && e.slug === slug);
}
```

- [ ] **Step 2: Create src/registry/index.ts**

```ts
export * from './types';
export {
  experiments,
  byCategory,
  findExperiment,
} from './loadExperiments';
```

- [ ] **Step 3: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors. (Empty `experiments/*/*/` means `modules` is `{}`, which is valid.)

- [ ] **Step 4: Commit**

```bash
git add src/registry/loadExperiments.ts src/registry/index.ts
git commit -m "feat(registry): implement glob-based experiment loader"
```

---

## Task 9: StatusBadge component

**Files:**
- Create: `src/components/StatusBadge.tsx`

- [ ] **Step 1: Create src/components/StatusBadge.tsx**

```tsx
import type { Status } from '@/registry/types';

const labels: Record<Status, string> = {
  wip: 'WIP',
  done: 'Done',
  archived: 'Archived',
};

const styles: Record<Status, string> = {
  wip: 'bg-yellow-500/15 text-yellow-300 border-yellow-500/30',
  done: 'bg-emerald-500/15 text-emerald-300 border-emerald-500/30',
  archived: 'bg-zinc-500/15 text-zinc-300 border-zinc-500/30',
};

interface Props {
  status: Status;
}

export function StatusBadge({ status }: Props) {
  return (
    <span
      className={`inline-flex items-center rounded-md border px-2 py-0.5 text-xs font-medium ${styles[status]}`}
    >
      {labels[status]}
    </span>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/StatusBadge.tsx
git commit -m "feat(ui): add StatusBadge component"
```

---

## Task 10: CategoryTabs component

**Files:**
- Create: `src/components/CategoryTabs.tsx`

- [ ] **Step 1: Create src/components/CategoryTabs.tsx**

```tsx
import { CATEGORIES, CATEGORY_LABELS, type Category } from '@/registry/types';

interface Props {
  active: Category;
  onChange: (cat: Category) => void;
}

export function CategoryTabs({ active, onChange }: Props) {
  return (
    <div role="tablist" className="flex gap-1 border-b border-border">
      {CATEGORIES.map((cat) => {
        const isActive = cat === active;
        return (
          <button
            key={cat}
            type="button"
            role="tab"
            aria-selected={isActive}
            onClick={() => onChange(cat)}
            className={
              isActive
                ? 'border-b-2 border-accent px-4 py-2 text-sm font-medium text-ink'
                : 'border-b-2 border-transparent px-4 py-2 text-sm font-medium text-ink-muted hover:text-ink'
            }
          >
            {CATEGORY_LABELS[cat]}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/CategoryTabs.tsx
git commit -m "feat(ui): add CategoryTabs component"
```

---

## Task 11: ExperimentCard component

**Files:**
- Create: `src/components/ExperimentCard.tsx`

- [ ] **Step 1: Create src/components/ExperimentCard.tsx**

```tsx
import { Link } from 'react-router-dom';
import type { ExperimentMeta } from '@/registry/types';
import { StatusBadge } from './StatusBadge';

interface Props {
  experiment: ExperimentMeta;
}

export function ExperimentCard({ experiment }: Props) {
  const { category, slug, title, description, status, tags } = experiment;
  return (
    <Link
      to={`/${category}/${slug}`}
      className="group flex flex-col gap-3 rounded-lg border border-border bg-surface-2 p-5 transition hover:-translate-y-0.5 hover:border-accent hover:shadow-lg"
    >
      <div className="flex items-start justify-between gap-2">
        <h3 className="text-base font-semibold text-ink group-hover:text-accent">
          {title}
        </h3>
        <StatusBadge status={status} />
      </div>
      <p className="line-clamp-2 text-sm text-ink-muted">{description}</p>
      <div className="mt-auto flex flex-wrap gap-1">
        {tags.map((tag) => (
          <span
            key={tag}
            className="rounded bg-surface px-2 py-0.5 text-xs text-ink-muted"
          >
            #{tag}
          </span>
        ))}
      </div>
    </Link>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/ExperimentCard.tsx
git commit -m "feat(ui): add ExperimentCard component"
```

---

## Task 12: Layout component

**Files:**
- Create: `src/app/Layout.tsx`

- [ ] **Step 1: Create src/app/Layout.tsx**

```tsx
import { Suspense } from 'react';
import { Link, Outlet, useLocation, useParams } from 'react-router-dom';
import { byCategory } from '@/registry';
import type { Category } from '@/registry/types';
import { CATEGORY_LABELS } from '@/registry/types';

export function Layout() {
  const location = useLocation();
  const params = useParams<{ category?: Category; slug?: string }>();
  const isHome = location.pathname === '/';
  const siblings = params.category ? byCategory(params.category) : [];

  return (
    <div className="flex min-h-full flex-col">
      <header className="border-b border-border bg-surface-2">
        <div className="mx-auto flex max-w-7xl items-center gap-4 px-6 py-3">
          <Link to="/" className="text-sm font-bold text-ink">
            CSII · UI Lab
          </Link>
          <span className="text-xs text-ink-muted">
            公司 UI 實驗 &amp; 切版練習
          </span>
        </div>
      </header>

      <div className="mx-auto flex w-full max-w-7xl flex-1 gap-6 px-6 py-6">
        {!isHome && params.category && (
          <aside className="w-56 shrink-0">
            <div className="mb-2 text-xs font-semibold uppercase tracking-wider text-ink-muted">
              {CATEGORY_LABELS[params.category]}
            </div>
            <nav className="flex flex-col gap-1">
              {siblings.map((exp) => {
                const active = exp.slug === params.slug;
                return (
                  <Link
                    key={exp.slug}
                    to={`/${exp.category}/${exp.slug}`}
                    className={
                      active
                        ? 'rounded-md border-l-2 border-accent bg-surface-2 px-3 py-1.5 text-sm text-ink'
                        : 'rounded-md border-l-2 border-transparent px-3 py-1.5 text-sm text-ink-muted hover:text-ink'
                    }
                  >
                    {exp.title}
                  </Link>
                );
              })}
            </nav>
          </aside>
        )}

        <main className="min-w-0 flex-1">
          <Suspense
            fallback={<div className="text-sm text-ink-muted">載入中…</div>}
          >
            <Outlet />
          </Suspense>
        </main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/app/Layout.tsx
git commit -m "feat(app): add Layout with header and conditional sidebar"
```

---

## Task 13: HomePage

**Files:**
- Create: `src/app/HomePage.tsx`

- [ ] **Step 1: Create src/app/HomePage.tsx**

```tsx
import { useState, useMemo } from 'react';
import { byCategory } from '@/registry';
import { CATEGORIES, CATEGORY_LABELS, type Category } from '@/registry/types';
import { CategoryTabs } from '@/components/CategoryTabs';
import { ExperimentCard } from '@/components/ExperimentCard';

const DEFAULT_CATEGORY: Category = CATEGORIES[0]!;

export function HomePage() {
  const [active, setActive] = useState<Category>(DEFAULT_CATEGORY);
  const items = useMemo(() => byCategory(active), [active]);

  return (
    <div className="flex flex-col gap-6">
      <div>
        <h1 className="text-2xl font-bold text-ink">CSII · UI Lab</h1>
        <p className="text-sm text-ink-muted">
          CSS 技巧練習、元件實驗、完整切版。瀏覽下方分類。
        </p>
      </div>

      <CategoryTabs active={active} onChange={setActive} />

      {items.length === 0 ? (
        <div className="rounded-lg border border-dashed border-border p-10 text-center text-sm text-ink-muted">
          這個分類（{CATEGORY_LABELS[active]}）還沒有實驗。
        </div>
      ) : (
        <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {items.map((exp) => (
            <ExperimentCard key={`${exp.category}/${exp.slug}`} experiment={exp} />
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/app/HomePage.tsx
git commit -m "feat(app): add HomePage with tabs and grid"
```

---

## Task 14: NotFound page

**Files:**
- Create: `src/app/NotFound.tsx`

- [ ] **Step 1: Create src/app/NotFound.tsx**

```tsx
import { Link } from 'react-router-dom';

export function NotFound() {
  return (
    <div className="flex flex-col items-start gap-4 py-10">
      <h1 className="text-2xl font-bold text-ink">404</h1>
      <p className="text-sm text-ink-muted">找不到這個實驗。</p>
      <Link to="/" className="text-sm text-accent hover:underline">
        回首頁
      </Link>
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/app/NotFound.tsx
git commit -m "feat(app): add NotFound page"
```

---

## Task 15: Experiment route wrapper + router

**Files:**
- Create: `src/app/ExperimentRoute.tsx`
- Create: `src/app/router.tsx`
- Create: `src/app/App.tsx`
- Modify: `src/main.tsx`

- [ ] **Step 1: Create src/app/ExperimentRoute.tsx**

```tsx
import { useParams } from 'react-router-dom';
import { findExperiment } from '@/registry';
import { StatusBadge } from '@/components/StatusBadge';
import { NotFound } from './NotFound';

export function ExperimentRoute() {
  const { category, slug } = useParams<{ category: string; slug: string }>();
  const exp = category && slug ? findExperiment(category, slug) : undefined;
  if (!exp) return <NotFound />;
  const { Component, title, description, status, tags } = exp;

  return (
    <article className="flex flex-col gap-6">
      <header className="flex flex-col gap-2">
        <div className="flex items-center gap-3">
          <h1 className="text-2xl font-bold text-ink">{title}</h1>
          <StatusBadge status={status} />
        </div>
        <p className="text-sm text-ink-muted">{description}</p>
        <div className="flex flex-wrap gap-1">
          {tags.map((tag) => (
            <span
              key={tag}
              className="rounded bg-surface-2 px-2 py-0.5 text-xs text-ink-muted"
            >
              #{tag}
            </span>
          ))}
        </div>
      </header>
      <div className="rounded-lg border border-border bg-surface-2 p-6">
        <Component />
      </div>
    </article>
  );
}
```

- [ ] **Step 2: Create src/app/router.tsx**

```tsx
import { createBrowserRouter } from 'react-router-dom';
import { Layout } from './Layout';
import { HomePage } from './HomePage';
import { ExperimentRoute } from './ExperimentRoute';
import { NotFound } from './NotFound';

export const router = createBrowserRouter([
  {
    path: '/',
    element: <Layout />,
    children: [
      { index: true, element: <HomePage /> },
      { path: ':category/:slug', element: <ExperimentRoute /> },
      { path: '*', element: <NotFound /> },
    ],
  },
]);
```

- [ ] **Step 3: Create src/app/App.tsx**

```tsx
import { RouterProvider } from 'react-router-dom';
import { router } from './router';

export function App() {
  return <RouterProvider router={router} />;
}
```

- [ ] **Step 4: Replace src/main.tsx**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './app/App';
import './styles/globals.css';

const root = document.getElementById('root');
if (!root) throw new Error('Root element not found');

createRoot(root).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 5: Verify dev server**

Run: `npm run dev`
Expected:
- Homepage loads, shows three tabs (CSS Techniques / Layouts / Components)
- All three tabs show the empty-state message (no experiments yet)
- Manually navigating to `/css-techniques/nonexistent` shows NotFound
Kill server.

- [ ] **Step 6: Commit**

```bash
git add src/app/ExperimentRoute.tsx src/app/router.tsx src/app/App.tsx src/main.tsx
git commit -m "feat(app): wire up router with registry"
```

---

## Task 16: First experiment — border-image assets

**Files:**
- Already exist (provided by user): `src/experiments/css-techniques/border-image/assets/corners/corner-rounded-revert-{lt,rt,lb,rb}.svg`
- Create: `src/experiments/css-techniques/border-image/assets/frame.svg` (9-slice composite)
- Create: `src/experiments/layouts/.gitkeep`
- Create: `src/experiments/components/.gitkeep`

Context: The user supplied 4 corner SVGs (18×18 each) with concave rounded corner paths in green fill + red stroke. This task composes them into a single 54×54 9-slice SVG for `border-image`. Slice: 18. The 4 corners hold the user's corner art; the 4 middle edges have 3-dot patterns so `stretch` / `repeat` / `round` will render visibly different edge behavior.

- [ ] **Step 1: Create the composite 9-slice frame.svg**

Create `src/experiments/css-techniques/border-image/assets/frame.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 54 54" width="54" height="54">
  <!-- Top-left corner (user's lt) -->
  <path d="M18,2h-3.53c-.26,6.78-5.7,12.22-12.47,12.47v3.53"
    fill="#0be394" stroke="red" stroke-miterlimit="10" stroke-width="4"/>
  <!-- Top-right corner (user's rt, translated) -->
  <g transform="translate(36,0)">
    <path d="M16,18v-3.53c-6.78-.26-12.22-5.7-12.47-12.47H0"
      fill="#0be394" stroke="red" stroke-miterlimit="10" stroke-width="4"/>
  </g>
  <!-- Bottom-left corner (user's lb, translated) -->
  <g transform="translate(0,36)">
    <path d="M2,0v3.53c6.78.26,12.22,5.7,12.47,12.47h3.53"
      fill="#0be394" stroke="red" stroke-miterlimit="10" stroke-width="4"/>
  </g>
  <!-- Bottom-right corner (user's rb, translated) -->
  <g transform="translate(36,36)">
    <path d="M0,16h3.53c.26-6.78,5.7-12.22,12.47-12.47V0"
      fill="#0be394" stroke="red" stroke-miterlimit="10" stroke-width="4"/>
  </g>
  <!-- Top middle edge: 3 red dots -->
  <g transform="translate(18,0)">
    <circle cx="3" cy="9" r="2" fill="red"/>
    <circle cx="9" cy="9" r="2" fill="red"/>
    <circle cx="15" cy="9" r="2" fill="red"/>
  </g>
  <!-- Bottom middle edge -->
  <g transform="translate(18,36)">
    <circle cx="3" cy="9" r="2" fill="red"/>
    <circle cx="9" cy="9" r="2" fill="red"/>
    <circle cx="15" cy="9" r="2" fill="red"/>
  </g>
  <!-- Left middle edge -->
  <g transform="translate(0,18)">
    <circle cx="9" cy="3" r="2" fill="red"/>
    <circle cx="9" cy="9" r="2" fill="red"/>
    <circle cx="9" cy="15" r="2" fill="red"/>
  </g>
  <!-- Right middle edge -->
  <g transform="translate(36,18)">
    <circle cx="9" cy="3" r="2" fill="red"/>
    <circle cx="9" cy="9" r="2" fill="red"/>
    <circle cx="9" cy="15" r="2" fill="red"/>
  </g>
</svg>
```

- [ ] **Step 2: Create placeholder .gitkeep files**

Create `src/experiments/layouts/.gitkeep` (empty file).
Create `src/experiments/components/.gitkeep` (empty file).

- [ ] **Step 3: Commit**

```bash
git add src/experiments/css-techniques/border-image/assets/frame.svg src/experiments/css-techniques/border-image/assets/corners/ src/experiments/layouts/.gitkeep src/experiments/components/.gitkeep
git commit -m "feat(experiments): add border-image frame asset + category placeholders"
```

---

## Task 17: First experiment — border-image demo

**Files:**
- Create: `src/experiments/css-techniques/border-image/styles.module.css`
- Create: `src/experiments/css-techniques/border-image/index.tsx`
- Create: `src/experiments/css-techniques/border-image/meta.ts`
- Create: `src/experiments/css-techniques/border-image/README.md`

- [ ] **Step 1: Create styles.module.css**

```css
.demo {
  display: block;
  width: 220px;
  height: 140px;
  background: var(--color-surface);
  border-width: 18px;
  border-style: solid;
  border-color: transparent;
  border-image-source: url('./assets/frame.svg');
  border-image-slice: 18;
  border-image-width: 18px;
  border-image-outset: 0;
}

.stretch {
  border-image-repeat: stretch;
}

.repeat {
  border-image-repeat: repeat;
}

.round {
  border-image-repeat: round;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, 220px);
  gap: 2rem;
}

.label {
  margin-bottom: 0.5rem;
  font-size: 0.75rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--color-ink-muted);
}

.code {
  margin-top: 0.5rem;
  padding: 0.5rem 0.75rem;
  font-family: var(--font-mono, monospace);
  font-size: 0.75rem;
  color: var(--color-ink-muted);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 0.375rem;
  white-space: pre;
  overflow-x: auto;
}
```

- [ ] **Step 2: Create index.tsx**

```tsx
import styles from './styles.module.css';

const modes = [
  { key: 'stretch', className: styles.stretch, css: 'border-image-repeat: stretch;' },
  { key: 'repeat', className: styles.repeat, css: 'border-image-repeat: repeat;' },
  { key: 'round', className: styles.round, css: 'border-image-repeat: round;' },
] as const;

export default function BorderImageDemo() {
  return (
    <div className={styles.grid}>
      {modes.map(({ key, className, css }) => (
        <div key={key}>
          <div className={styles.label}>{key}</div>
          <div className={`${styles.demo} ${className}`} />
          <pre className={styles.code}>{css}</pre>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: Create meta.ts**

```ts
import { lazy } from 'react';
import type { ExperimentMeta } from '@/registry/types';

const meta: Omit<ExperimentMeta, 'slug' | 'category'> = {
  title: 'Border Image',
  description: 'CSS border-image 的 stretch / repeat / round 三種模式對照',
  status: 'wip',
  tags: ['css', 'border', 'svg'],
  createdAt: '2026-04-15',
  Component: lazy(() => import('./index')),
};

export default meta;
```

- [ ] **Step 4: Create README.md**

```md
# Border Image

## 目的
學會 CSS `border-image` 的四個關鍵屬性（source / slice / width / outset）
與 repeat 模式（stretch / repeat / round / space）如何互動。

## Demo 內容
同一張 90×90 的 SVG 邊框圖，切成 30 像素的 9-slice，套用到三個相同尺寸的
容器上，分別使用 `stretch` / `repeat` / `round` 模式。肉眼可直接對比差異。

## 關鍵發現
- `border-image-slice` 的數字沒有單位 = 像素；加 `%` 則是百分比
- 四個角永遠不會重複或縮放；差異在上下左右四條邊
- `stretch`：邊條被拉伸填滿，可能失真
- `repeat`：邊條原尺寸重複，邊緣可能被切斷
- `round`：自動微調 tile 寬度讓整條邊剛好放整數個 tile，不切斷
- 要讓 border-image 顯示，容器必須同時有 `border-style`（即使是 transparent）

## 瀏覽器支援
Chrome / Edge / Firefox / Safari 全支援，近 10 年版本皆可。

## 相關連結
- MDN: https://developer.mozilla.org/en-US/docs/Web/CSS/border-image
- Can I Use: https://caniuse.com/border-image

## 踩雷紀錄
（隨實驗過程補充）

## 下一步
- v2：加 slider 互動控制 width / slice / outset
- v3：切換多組 source 圖片
```

- [ ] **Step 5: Verify in dev server**

Run: `npm run dev`
Expected:
- Homepage: CSS Techniques tab now shows one card: "Border Image"
- Click the card → navigates to `/css-techniques/border-image`
- Page shows: title + WIP badge + tags, then a container with three labelled demo boxes
- Each box shows a visibly different border pattern (the `round` one should have cleanly aligned tiles on edges)
- Sidebar appears on left with "Border Image" highlighted
Kill server.

- [ ] **Step 6: Commit**

```bash
git add src/experiments/css-techniques/border-image/
git commit -m "feat(experiments): add border-image stretch/repeat/round demo"
```

---

## Task 18: Top-level README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Create README.md**

```md
# CSII · UI Lab

個人 UI 實驗場。CSS 技巧、元件實驗、公司設計稿切版練習。

## 開發

```bash
npm install
npm run dev        # http://localhost:5173
```

## Scripts

| 指令 | 用途 |
|------|------|
| `npm run dev` | 啟動開發伺服器 |
| `npm run build` | 型別檢查 + 產出 dist/ |
| `npm run preview` | 預覽 build 產物 |
| `npm run lint` | ESLint |
| `npm run format` | Prettier 格式化 |
| `npm test` | Vitest 單元測試 |

## 專案結構

```
src/
├─ app/          Shell：Router / Layout / HomePage / NotFound
├─ registry/     自動載入 experiments/ 下的所有實驗
├─ components/   Shell 用的共用元件
├─ experiments/  所有實驗（分三類）
│  ├─ css-techniques/
│  ├─ layouts/
│  └─ components/
└─ styles/       globals.css + theme.css
```

## 如何新增一個實驗

1. 在 `src/experiments/<category>/<slug>/` 建資料夾，其中：
   - `<category>` 是 `css-techniques` / `layouts` / `components` 其中之一
   - `<slug>` 是路由 URL 用的英數小寫連字符，例：`border-image`
2. 至少放這三個檔案：
   - `meta.ts` — 匯出 `ExperimentMeta`（參考 [border-image 範例](src/experiments/css-techniques/border-image/meta.ts)）
   - `index.tsx` — 預設匯出 React 元件，就是這個實驗的 demo
   - `README.md` — 目的、關鍵發現、踩雷紀錄
3. 需要 CSS Modules 就加 `styles.module.css`，圖片放 `assets/`
4. 儲存即可：Registry 會在下次 build / dev reload 自動掃到

無需改動路由表或索引。

## 技術棧

Vite 5 · React 18 · TypeScript (strict) · Tailwind v3 · CSS Modules · React Router v6 · Vitest · ESLint · Prettier · Husky

## 設計文件

[docs/superpowers/specs/2026-04-15-ui-lab-design.md](docs/superpowers/specs/2026-04-15-ui-lab-design.md)
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add top-level README"
```

---

## Task 19: Final smoke test

- [ ] **Step 1: Run full test suite**

Run: `npm test -- --run`
Expected: All 5 parseExperimentPath tests pass.

- [ ] **Step 2: Run lint**

Run: `npm run lint`
Expected: No errors.

- [ ] **Step 3: Run build**

Run: `npm run build`
Expected:
- `tsc --noEmit` passes with no type errors
- `vite build` completes, outputs to `dist/`
- No warnings about missing dependencies or type errors

- [ ] **Step 4: Preview build**

Run: `npm run preview`
Expected:
- Preview server starts on http://localhost:4173
- Homepage loads, CSS Techniques tab shows Border Image card
- Navigating to the experiment renders the three border-image modes correctly
- Browser console: no errors, no warnings
Kill server.

- [ ] **Step 5: Test pre-commit hook**

Make a trivial edit (e.g., add blank line to README.md), then commit:

```bash
git add README.md
git commit -m "chore: verify hook works"
```

Expected: lint-staged runs, prettier runs, commit succeeds.

- [ ] **Step 6: Log final state**

Run: `git log --oneline`
Expected: ~20 commits, one per task.

Run: `git status`
Expected: `working tree clean`.

---

## Completion checklist

- [ ] `npm run dev` starts and homepage renders
- [ ] Three tabs work, empty categories show empty-state
- [ ] Border Image card appears under CSS Techniques
- [ ] Clicking card navigates to experiment page
- [ ] Three border modes render visibly differently
- [ ] Sidebar shows when in experiment page
- [ ] `npm run build` succeeds with zero warnings/errors
- [ ] `npm test` — all tests pass
- [ ] `npm run lint` — zero errors
- [ ] Pre-commit hook fires on commit
- [ ] README.md explains how to add a new experiment
